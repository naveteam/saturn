import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'

import { Button, SaturnProvider } from '../components'

<Meta title='Data Input/Button' component={Button} />

# Button

O componente `Button` é utilizado para permitir que o usuário possa realizar ações dentro da aplicação.

<Canvas>
  <Story
    name='Base'
    parameters={{
      design: {
        type: 'figma',
        url: 'https://www.figma.com/file/O3bKxIcsj2rc1FNIRclJyT/Design-System?node-id=238%3A0'
      }
    }}
  >
    <SaturnProvider>
      <Button width={1 / 4} caption='Base' />
    </SaturnProvider>
  </Story>
</Canvas>

# Color

O `Button` pode receber a prop `color`. A variant `primary` tem uma função de mais destaque, ideal para executar ações e/ou mostrar informações fundamentais para o usuário. A variant `secondary` é ideal para funções de menores destaque. Através da prop `captionColor` é possível alterar a cor de caption.

<Canvas>
  <Story
    name='Color'
    parameters={{
      design: {
        type: 'figma',
        url: 'https://www.figma.com/file/O3bKxIcsj2rc1FNIRclJyT/Design-System?node-id=238%3A0'
      }
    }}
  >
    <SaturnProvider>
      <Button width={1 / 4} caption='Primary' />
      <Button width={1 / 4} color='secondary' caption='Secondary' />
      <Button width={1 / 4} color='disabled' caption='Caption Color' captionColor='gray.800' />
    </SaturnProvider>
  </Story>
</Canvas>

# Variant

Em conjunto com a prop `color`, o `Button` pode receber também a prop `variant`. As variants podem criar botões preenchidos, apenas contornados ou apenas o texto.

<Canvas>
  <Story
    name='Variants'
    parameters={{
      design: {
        type: 'figma',
        url: 'https://www.figma.com/file/O3bKxIcsj2rc1FNIRclJyT/Design-System?node-id=238%3A0'
      }
    }}
  >
    <SaturnProvider>
      <Button width={1 / 3} variant='filled' caption='Filled' />
      <Button width={1 / 3} variant='outlined' caption='Outlined' />
      <Button width={1 / 3} variant='text' caption='Text' />
    </SaturnProvider>
  </Story>
</Canvas>

# Disabled

A prop `disabled` desabilita completamente o botão, impedindo seu uso.

<Canvas>
  <Story
    name='Disabled'
    parameters={{
      design: {
        type: 'figma',
        url: 'https://www.figma.com/file/O3bKxIcsj2rc1FNIRclJyT/Design-System?node-id=238%3A0'
      }
    }}
  >
    <SaturnProvider>
      <Button width={1 / 3} variant='filled' caption='Filled Disabled' disabled />
      <Button width={1 / 3} variant='outlined' caption='Outlined Disabled' disabled />
      <Button width={1 / 3} variant='text' caption='Text Disabled' disabled />
    </SaturnProvider>
  </Story>
</Canvas>

<ArgsTable of={Button} />
