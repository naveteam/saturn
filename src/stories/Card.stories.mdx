import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'

import { ThemeProvider } from '../theme'
import { Card } from '../components'

<Meta title='Data Display/Card' component={Card} />

# Card

O componente `Card` fica envolto pelo `CardContainer`, que contém a borda com sombra. Para conteúdo de mídia, temos o `MediaContainer`.

<Canvas>
  <Story
    name='Base'
    parameters={{
      design: {
        type: 'figma',
        url: 'https://www.figma.com/file/O3bKxIcsj2rc1FNIRclJyT/Saturn-System?node-id=806%3A11347'
      }
    }}
  >
    <ThemeProvider>
      <Card headerText='Header Text' complementaryText='Complementary text' />
    </ThemeProvider>
  </Story>
</Canvas>
