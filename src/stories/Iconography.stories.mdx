import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'

import { Icon, Flex, Box, Caption } from '../components'
import { ThemeProvider } from '../theme'
import { iconNames } from '../utils'

<Meta title='Data Display/Iconography' component={Icon} argTypes={{ icon: { control: {
  type: 'select',
  options: iconNames
} }, color: { control: 'color', options: iconNames } }} />

export const IconBase = args => (
  <ThemeProvider>
    <Icon {...args} />
  </ThemeProvider>
)


# Icon

O componente de `Icon` é responsável por apresentar um dos ícones definidos em nosso Design System. Ele recebe um parâmetro principal chamado `icon` que define qual icon será renderizado.

<Story
  name='Base'
  args={{ icon: 'face' }}
  parameters={{
    design: {
      type: 'figma',
      url: 'https://www.figma.com/file/O3bKxIcsj2rc1FNIRclJyT/Saturn-System?node-id=18%3A0'
    }
  }}
>
{IconBase.bind({})}
</Story>

# All Icons

Abaixo segue a lista com todos os ícones disponíveis no Design System até o momento.

<Canvas>
  <Story
    name='All Icons'
    parameters={{
      design: {
        type: 'figma',
        url: 'https://www.figma.com/file/O3bKxIcsj2rc1FNIRclJyT/Saturn-System?node-id=18%3A0'
      }
    }}
  >
    <ThemeProvider>
      <Flex flexWrap='wrap'>
        {iconNames.map(iconName => (
          <Flex width={1 / 8} alignItems='center' flexDirection='column' margin='10px 0'>
            <Icon icon={iconName} />
            <Caption>{iconName}</Caption>
          </Flex>
        ))}
      </Flex>
    </ThemeProvider>
  </Story>
</Canvas>

