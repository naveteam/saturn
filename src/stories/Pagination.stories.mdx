import { useState } from 'react'
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks'

import { Pagination } from '../components'
import { ThemeProvider } from '../theme'

<Meta title='Data input|Pagination' component={Pagination} />

# Pagination
export const PaginationProps = () => {
  const [page, setPage] = useState(1)
  return <Pagination page={page} pageSize={12} onPageChange={setPage} />
}

O componente de `Pagination` é utilizado para que o usuário possa trocar de página em sua aplicação. Sua estrutura é composta por um botões e dois ícones do tipo arrow.

<Preview>
  <Story
    name='Pagination'
    parameters={{
      design: {
        type: 'figma',
        url: 'https://www.figma.com/file/O3bKxIcsj2rc1FNIRclJyT/Saturn-System?node-id=257%3A566'
      }
    }}
  >
    <ThemeProvider>
      <PaginationProps />
    </ThemeProvider>
  </Story>
</Preview>

# Pagination with INput

Caso haja mais de 100 páginas a prop `variant` deve ser passada, caso seja a estrutura do componente será um input.

<Preview>
  <Story
    name='Input'
    parameters={{
      design: {
        type: 'figma',
        url: 'https://www.figma.com/file/O3bKxIcsj2rc1FNIRclJyT/Saturn-System?node-id=257%3A566'
      }
    }}
  >
    <ThemeProvider>
      <Pagination page={1} pageSize={200} variant/>
    </ThemeProvider>
  </Story>
</Preview>
