import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks'
import { RadioButton } from '../components'
import { ThemeProvider } from '../theme'

<Meta title='Data Input/Radio Button' component={RadioButton} />

# RadioButton

O componente `Radio Button` deve ser utilizado em casos no qual o usuário pode selecionar apenas uma opção dentre uma lista de opções. Em sua versão desktop os Radio Buttons são mostrados lado a lado. No mobile, os Radio Buttons são mostrados verticalmente. Em suas props padrões, o componente está habilitado e desmarcado.

<Canvas>
  <Story
    name='Base'
    parameters={{
      design: {
        type: 'figma',
        url: 'https://www.figma.com/file/O3bKxIcsj2rc1FNIRclJyT/Saturn-System?node-id=239%3A470'
      }
    }}
  >
    <ThemeProvider>
      <RadioButton label='Base' />
    </ThemeProvider>
  </Story>
</Canvas>

# Checked | onChange

O `Radio Button` pode receber a prop `checked`. É um valor booleano que indica o valor inicial para um `Radio Button`, por padrão o valor é `false`. Se o valor `checked` for passado para o `Radio Button`, será necessário também passar uma função de onChange para mudar o seu valor, caso contrário, o valor de checked nunca irá mudar. Se o componente estiver desabilitado, não será necessário passar uma função de `onChange`.

<Canvas>
  <Story
    name='Checked | onChange'
    parameters={{
      design: {
        type: 'figma',
        url: 'https://www.figma.com/file/O3bKxIcsj2rc1FNIRclJyT/Saturn-System?node-id=239%3A470'
      }
    }}
  >
    <ThemeProvider>
      <RadioButton checked={true} onChange={() => {}} label='Selected' />
      <RadioButton checked={false} onChange={() => {}} label='Not selected' />
      <RadioButton checked={true} disabled={true} label='Selected disabled' />
      <RadioButton checked={false} disabled={true} label='Not selected disabled' />
    </ThemeProvider>
  </Story>
</Canvas>

# Disabled

A prop `disabled` desabilita completamente o `Radio Button`, impedindo seu uso e as cores customizadas não irão funcionar.

<Canvas>
  <Story
    name='Disabled'
    parameters={{
      design: {
        type: 'figma',
        url: 'https://www.figma.com/file/O3bKxIcsj2rc1FNIRclJyT/Saturn-System?node-id=239%3A470'
      }
    }}
  >
    <ThemeProvider>
      <RadioButton label='Not selected disabled' disabled={true} name='radio13' />
      <RadioButton checked={true} label='Selected disabled' disabled={true} name='radio14' />
    </ThemeProvider>
  </Story>
</Canvas>

# Label

O `Radio Button` aceita uma label opcional para cada elemento, se não for fornecido, nada será exibido.

<Canvas>
  <Story
    name='label'
    parameters={{
      design: {
        type: 'figma',
        url: 'https://www.figma.com/file/O3bKxIcsj2rc1FNIRclJyT/Saturn-System?node-id=239%3A470'
      }
    }}
  >
    <ThemeProvider>
      <RadioButton label='RadioButton Label' name='radio15' />
      <RadioButton label='RadioButton' name='radio15' />
    </ThemeProvider>
  </Story>
</Canvas>

<ArgsTable of={RadioButton} />
